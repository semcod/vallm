# vallm/flow | 316 func | 12 pipelines | 8 hub-types | 2026-03-23

PIPELINES[12] (Analysis:2, Core:3, Export:2, IO:1, NLP:1, Unknown:3):
  Src [Analysis]: Proposal → Optional[Language]
              → validate(proposal:Proposal, context:dict) → ValidationResult CC=2    mutation ▶
              → _check_lizard(code:str, language:str, filename:str | None) → tuple[float, list[Issue], dict] CC=12   mutation
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: _check_lizard(CC=12)

  Src [Core]: Proposal → ?
              → validate(proposal:Proposal, context:dict) → ValidationResult CC=2    pure ▶
              → _validate_treesitter(code:str, language:str) → tuple[float, list[Issue]] CC=3    mutation
              → tree_sitter_error_count(code:str, language:str) → int CC=1    mutation
              → parse_code(code:str, language:str)      CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: _validate_treesitter(CC=3)

  Src [Unknown]: Proposal → Optional[int]
              → validate(proposal:Proposal, context:dict) → ValidationResult CC=3    cache ▶
              → _parse_response(response_text:str) → ValidationResult CC=4    IO
              → _parse_issues(data:dict) → list         CC=3    mutation
              → _parse_line_number(line) → Optional[int] CC=4    pure ■
              PURITY: 1/4 pure  BOTTLENECK: _parse_response(CC=4)

  Src [Core]: Path | str → str
              → matches(path:Path | str) → bool         CC=4    pure ▶
              → _match_pattern(rel_path:str, name:str, pattern:str) → bool CC=8    pure
              → _fnmatch(name:str, pattern:str) → bool  CC=1    pure
              → _pattern_to_regex(pattern:str) → str    CC=11   mutation ■
              PURITY: 3/4 pure  BOTTLENECK: _pattern_to_regex(CC=11)

  Src [Export]: dict → str
              → output_batch_results(results_by_language:dict, filtered_files:list, passed_count:int, failed_files:list, output_format:str) → None CC=4    pure ▶
              → output_batch_toon(results_by_language:dict, filtered_files:list, passed_count:int, failed_files:list) → None CC=17   IO  !!
              → build_failed_files_data(failed_files:list) → list CC=2    pure
              → format_error_message(error:str) → str   CC=3    pure ■
              PURITY: 3/4 pure  BOTTLENECK: output_batch_toon(CC=17)

  Src [Export]: list[Path] → None
              → process_batch(paths:list[Path], recursive:bool, include:Optional[str], exclude:Optional[str], use_gitignore:bool, settings:VallmSettings, output_format:str, fail_fast:bool, verbose:bool, show_issues:bool) → tuple[dict, list, int] CC=2    pure ▶
              → _process_files(filtered_files:list[Path], settings:VallmSettings, output_format:str, fail_fast:bool, verbose:bool, show_issues:bool) → tuple[dict, list, int, list] CC=24   IO  !!
              → output_validate_result(result:ValidationResult, output_format:str, verbose:bool) → None CC=3    pure
              → output_rich(result:ValidationResult, verbose:bool) → None CC=6    IO ■
              PURITY: 2/4 pure  BOTTLENECK: _process_files(CC=24)

  Src [Core]: ? → ?
              → main()                                  CC=9    IO ▶
              → diff_python_code(before_code:str, after_code:str) → GraphDiffResult CC=1    pure
              → diff_graphs(before:CodeGraph, after:CodeGraph) → GraphDiffResult CC=5    pure
              → _diff_list(before, after, added:bool)   CC=9    pure ■
              PURITY: 3/4 pure  BOTTLENECK: main(CC=9)

  Examples [Unknown]: ? → ?
              → main()                                  CC=4    IO ▶
              → run_autonomous_workflow(code_path:Path, max_iterations:int) CC=9    IO
              → run_runtime_tests(code_path:Path, test_file:Optional[Path]) → Dict CC=8    IO
              → create_basic_tests(code_path:Path, test_file:Path) CC=7    IO ■
              PURITY: 0/4 pure  BOTTLENECK: run_autonomous_workflow(CC=9)

  Examples [Unknown]: ? → ?
              → main()                                  CC=5    IO ▶
              → run_simple_workflow(code_path:Path, max_iterations:int) CC=9    IO
              → run_simple_test(code:str) → Dict        CC=2    IO
              → log_step(step:int, description:str)     CC=1    IO ■
              PURITY: 0/4 pure  BOTTLENECK: run_simple_workflow(CC=9)

  Examples [NLP]: ? → Optional[Language]
              → main()                                  CC=11   IO ▶
              → test_language_detection()               CC=4    IO
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: main(CC=11)

  Examples [Analysis]: ? → ?
              → main()                                  CC=3    IO ▶
              → run_mcp_workflow(code_path:Path, max_iterations:int) CC=5    IO
              → analyze_with_code2llm(code_path:Path) → dict CC=9    IO
              → log_step(step:int, description:str)     CC=1    IO ■
              PURITY: 0/4 pure  BOTTLENECK: analyze_with_code2llm(CC=9)

  Examples [IO]: ? → Optional[Language]
              → main()                                  CC=2    IO ▶
              → analyze_with_code2llm(project_path:Path) → dict CC=8    IO
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: analyze_with_code2llm(CC=8)


TRANSFORMS (fan-out ≥10):
  run_autonomous_workflow(code_path:Path, max_iterations:int) fan=19   fan=19
  analyze_with_code2llm(code_path:Path) → Dict            fan=17   fan=17
  analyze_with_code2llm(code_path:Path) → dict            fan=16   fan=16
  run_mcp_workflow(code_path:Path, max_iterations:int)    fan=16   fan=16
  run_simple_workflow(code_path:Path, max_iterations:int) fan=15   fan=15
  extract_imports(code:str) → List[Dict[str, Any]]        fan=14   PIPELINE:JavaScriptImportValidator.entry
  extract_imports(code:str) → List[Dict[str, Any]]        fan=13   PIPELINE:GoImportValidator.entry
  extract_imports(code:str) → List[Dict[str, Any]]        fan=13   PIPELINE:RustImportValidator.entry
  main()                                                  fan=13   !! script-in-disguise
  analyze_with_code2llm(code_path:Path) → Dict            fan=13   fan=13
  _parse_ruff_text(output:str) → List[Issue]              fan=12   PIPELINE:LintValidator.entry
  _process_files(filtered_files:list[Path], settings:VallmSettings, output_format:str, fail_fast:bool, verbose:bool, show_issues:bool) → tuple[dict, list, int, list] fan=12   PIPELINE:BatchProcessor.entry
  main()                                                  fan=12   !! script-in-disguise
  main()                                                  fan=12   !! script-in-disguise
  main()                                                  fan=11   !! script-in-disguise

CONTRACTS:
  Pipeline: Src
    validate(proposal:Proposal, context:dict) → ValidationResult
      IN:  proposal:Proposal, context:dict
      OUT: ValidationResult
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _check_lizard(code:str, language:str, filename:str | None) → tuple[float, list[Issue], dict]
      IN:  code:str, language:str, filename:str | None
      OUT: tuple[float, list[Issue], dict]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]

  Pipeline: Src
    validate(proposal:Proposal, context:dict) → ValidationResult
      IN:  proposal:Proposal, context:dict
      OUT: ValidationResult
      INVARIANT: raises on invalid input

    _validate_treesitter(code:str, language:str) → tuple[float, list[Issue]]
      IN:  code:str, language:str
      OUT: tuple[float, list[Issue]]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    tree_sitter_error_count(code:str, language:str) → int
      IN:  code:str, language:str
      OUT: int
      SIDE-EFFECT: global(errors)

    parse_code(code:str, language:str)
      IN:  code:str, language:str
      OUT: None

  Pipeline: Src
    validate(proposal:Proposal, context:dict) → ValidationResult
      IN:  proposal:Proposal, context:dict
      OUT: ValidationResult
      SIDE-EFFECT: cache(self.cache.get, self.cache.set)
      INVARIANT: raises on invalid input

    _parse_response(response_text:str) → ValidationResult
      IN:  response_text:str
      OUT: ValidationResult
      SIDE-EFFECT: IO(loads)

    _parse_issues(data:dict) → list
      IN:  data:dict
      OUT: list
      SIDE-EFFECT: pure

    _parse_line_number(line) → Optional[int]
      IN:  line
      OUT: Optional[int]

  Pipeline: Src
    matches(path:Path | str) → bool
      IN:  path:Path | str
      OUT: bool

    _match_pattern(rel_path:str, name:str, pattern:str) → bool
      IN:  rel_path:str, name:str, pattern:str
      OUT: bool

    _fnmatch(name:str, pattern:str) → bool
      IN:  name:str, pattern:str
      OUT: bool

    _pattern_to_regex(pattern:str) → str
      IN:  pattern:str
      OUT: str
      SIDE-EFFECT: pure

  Pipeline: Src
    output_batch_results(results_by_language:dict, filtered_files:list, passed_count:int, failed_files:list, output_format:str) → None
      IN:  results_by_language:dict, filtered_files:list, passed_count:int, failed_files:list, output_format:str
      OUT: None

    output_batch_toon(results_by_language:dict, filtered_files:list, passed_count:int, failed_files:list) → None
      IN:  results_by_language:dict, filtered_files:list, passed_count:int, failed_files:list
      OUT: None
      SIDE-EFFECT: IO(print, print, print)
      SMELL: CC=17 → split

    build_failed_files_data(failed_files:list) → list
      IN:  failed_files:list
      OUT: list

    format_error_message(error:str) → str
      IN:  error:str
      OUT: str

  Pipeline: Src
    process_batch(paths:list[Path], recursive:bool, include:Optional[str], exclude:Optional[str], use_gitignore:bool, settings:VallmSettings, output_format:str, fail_fast:bool, verbose:bool, show_issues:bool) → tuple[dict, list, int]
      IN:  paths:list[Path], recursive:bool, include:Optional[str], exclude:Optional[str], use_gitignore:bool, settings:VallmSettings, output_format:str, fail_fast:bool, verbose:bool, show_issues:bool
      OUT: tuple[dict, list, int]

    _process_files(filtered_files:list[Path], settings:VallmSettings, output_format:str, fail_fast:bool, verbose:bool, show_issues:bool) → tuple[dict, list, int, list]
      IN:  filtered_files:list[Path], settings:VallmSettings, output_format:str, fail_fast:bool, verbose:bool, show_issues:bool
      OUT: tuple[dict, list, int, list]
      SIDE-EFFECT: IO(print, read_text, print)
      SMELL: CC=24 → split

    output_validate_result(result:ValidationResult, output_format:str, verbose:bool) → None
      IN:  result:ValidationResult, output_format:str, verbose:bool
      OUT: None
      INVARIANT: raises on invalid input

    output_rich(result:ValidationResult, verbose:bool) → None
      IN:  result:ValidationResult, verbose:bool
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

  Pipeline: Src
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    diff_python_code(before_code:str, after_code:str) → GraphDiffResult
      IN:  before_code:str, after_code:str
      OUT: GraphDiffResult

    diff_graphs(before:CodeGraph, after:CodeGraph) → GraphDiffResult
      IN:  before:CodeGraph, after:CodeGraph
      OUT: GraphDiffResult

    _diff_list(before, after, added:bool)
      IN:  before, after, added:bool
      OUT: None

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    run_autonomous_workflow(code_path:Path, max_iterations:int)
      IN:  code_path:Path, max_iterations:int
      OUT: None
      SIDE-EFFECT: IO(read_text, print, print)

    run_runtime_tests(code_path:Path, test_file:Optional[Path]) → Dict
      IN:  code_path:Path, test_file:Optional[Path]
      OUT: Dict
      SIDE-EFFECT: IO(print, print, print)

    create_basic_tests(code_path:Path, test_file:Path)
      IN:  code_path:Path, test_file:Path
      OUT: None
      SIDE-EFFECT: IO(read_text, write_text, print)

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    run_simple_workflow(code_path:Path, max_iterations:int)
      IN:  code_path:Path, max_iterations:int
      OUT: None
      SIDE-EFFECT: IO(read_text, print, print)

    run_simple_test(code:str) → Dict
      IN:  code:str
      OUT: Dict
      SIDE-EFFECT: IO(print, print)

    log_step(step:int, description:str)
      IN:  step:int, description:str
      OUT: None
      SIDE-EFFECT: IO(print)

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    test_language_detection()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    run_mcp_workflow(code_path:Path, max_iterations:int)
      IN:  code_path:Path, max_iterations:int
      OUT: None
      SIDE-EFFECT: IO(read_text, print, print)

    analyze_with_code2llm(code_path:Path) → dict
      IN:  code_path:Path
      OUT: dict
      SIDE-EFFECT: IO(print, print, print)

    log_step(step:int, description:str)
      IN:  step:int, description:str
      OUT: None
      SIDE-EFFECT: IO(print)

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    analyze_with_code2llm(project_path:Path) → dict
      IN:  project_path:Path
      OUT: dict
      SIDE-EFFECT: IO(print, print, print)

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]


DATA_TYPES (by cross-function usage) [242 annotated, 0 inferred / 316 functions]:
  str                  consumed:185 produced:45   !! HUB-TYPE → split interface
  dict                 consumed:36  produced:14   !! HUB-TYPE → split interface
  bool                 consumed:33  produced:16   !! HUB-TYPE → split interface
  Path                 consumed:34  produced:0    !! HUB-TYPE → split interface
  int                  consumed:22  produced:4    !! HUB-TYPE → split interface
  list                 consumed:19  produced:6    !! HUB-TYPE → split interface
  ValidationResult     consumed:5   produced:18 
  Proposal             consumed:16  produced:1    !! HUB-TYPE → split interface
  str, Any             consumed:6   produced:10 
  Dict                 consumed:7   produced:6  
  VallmSettings        consumed:11  produced:2    !! HUB-TYPE → split interface
  list[Path]           consumed:6   produced:2  
  float                consumed:4   produced:4  
  Issue                consumed:1   produced:4  
  Language             consumed:0   produced:4    narrow scope
  GitignoreParser      consumed:1   produced:2    narrow scope
  list[Issue]          consumed:0   produced:3    narrow scope
  list[str]            consumed:2   produced:1    narrow scope
  str | Path           consumed:3   produced:0    narrow scope
  ExecutionResult      consumed:0   produced:3    narrow scope

  HUB TYPES (consumed ≥10):
    str → 185 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    dict → 36 consumers → split into:
      - replace with typed alternatives (dataclass/TypedDict)
    bool → 33 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    Path → 34 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    int → 22 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    list → 19 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    Proposal → 16 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    VallmSettings → 11 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)

SIDE_EFFECTS:
  IO:        main, LogicalErrorValidator._check_pyflakes, SemanticCache.__init__, SemanticCache.get, SemanticCache.set, SemanticCache.clear, SecurityValidator._try_bandit, LintValidator._check_ruff, SemanticValidator._call_http, SemanticValidator._parse_response
  Cache:     SemanticCache.get_cache_stats, get_semantic_cache, clear_semantic_cache, SemanticValidator.__init__, SemanticValidator.validate
  Mutation:  _initialize_validators, _run_validation_pipeline, _get_default_validators, ComplexityValidator.__init__, ComplexityValidator.validate, ComplexityValidator._check_python_complexity, ComplexityValidator._check_lizard, LogicalErrorValidator.__init__, SecurityValidator.validate, SecurityValidator._check_patterns
  Pure:      bump_version, VallmSpec.validate_proposal, VallmSpec.get_validator_name, VallmSpec.get_validator_tier, Issue.__str__, compute_verdict, validate, _initialize_settings, _initialize_context, BaseValidator.validate

  PIPELINE PURITY:
    Src             ██░░ 50% pure
    Src             ██░░ 50% pure
    Src             █░░░ 25% pure
    Src             ███░ 75% pure
    Src             ███░ 75% pure
    Src             ██░░ 50% pure
    Src             ███░ 75% pure
    Examples        ░░░░ 0% pure
    Examples        ░░░░ 0% pure
    Examples        ██░░ 50% pure
    Examples        ░░░░ 0% pure
    Examples        ██░░ 50% pure
