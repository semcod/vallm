# vallm/flow | 223 func | 12 pipelines | 5 hub-types | 2026-03-22

PIPELINES[12] (Analysis:2, Core:3, IO:1, NLP:4, Unknown:2):
  Src [NLP]: Optional[str] → Optional[Language]
              → validate(code:Optional[str], file:Optional[Path], language:Optional[str], reference:Optional[Path], config:Optional[Path], enable_semantic:bool, enable_security:bool, model:Optional[str], output_format:str, verbose:bool) CC=18   IO  !! ▶
              → get_language_for_validation(source:str | Path, explicit:Optional[str]) → str CC=4    pure
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 3/4 pure  BOTTLENECK: validate(CC=18)

  Src [Analysis]: Proposal → Optional[Language]
              → validate(proposal:Proposal, context:dict) → ValidationResult CC=2    mutation ▶
              → _check_lizard(code:str, language:str, filename:str | None) → tuple[float, list[Issue], dict] CC=12   mutation
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: _check_lizard(CC=12)

  Src [Unknown]: Proposal → str
              → validate(proposal:Proposal, context:dict) → ValidationResult CC=2    pure ▶
              → _call_llm(prompt:str) → str             CC=3    pure
              → _call_ollama(prompt:str) → str          CC=2    pure
              → _call_http(prompt:str) → str            CC=1    IO ■
              PURITY: 3/4 pure  BOTTLENECK: _call_llm(CC=3)

  Src [Core]: Proposal → ?
              → validate(proposal:Proposal, context:dict) → ValidationResult CC=2    pure ▶
              → _validate_treesitter(code:str, language:str) → tuple[float, list[Issue]] CC=3    mutation
              → tree_sitter_error_count(code:str, language:str) → int CC=1    mutation
              → parse_code(code:str, language:str)      CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: _validate_treesitter(CC=3)

  Src [Core]: Path | str → str
              → matches(path:Path | str) → bool         CC=4    pure ▶
              → _match_pattern(rel_path:str, name:str, pattern:str) → bool CC=8    pure
              → _fnmatch(name:str, pattern:str) → bool  CC=1    pure
              → _pattern_to_regex(pattern:str) → str    CC=11   mutation ■
              PURITY: 3/4 pure  BOTTLENECK: _pattern_to_regex(CC=11)

  Src [Core]: ? → ?
              → main()                                  CC=9    IO ▶
              → diff_python_code(before_code:str, after_code:str) → GraphDiffResult CC=1    pure
              → diff_graphs(before:CodeGraph, after:CodeGraph) → GraphDiffResult CC=5    pure
              → _diff_list(before, after, added:bool)   CC=6    pure ■
              PURITY: 3/4 pure  BOTTLENECK: main(CC=9)

  Examples [Unknown]: ? → ?
              → main()                                  CC=5    IO ▶
              → run_simple_workflow(code_path:Path, max_iterations:int) CC=9    IO
              → run_simple_test(code:str) → Dict        CC=2    IO
              → log_step(step:int, description:str)     CC=1    IO ■
              PURITY: 0/4 pure  BOTTLENECK: run_simple_workflow(CC=9)

  Examples [NLP]: ? → Optional[Language]
              → main()                                  CC=11   IO ▶
              → test_language_detection()               CC=4    IO
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: main(CC=11)

  Examples [IO]: ? → Optional[Language]
              → main()                                  CC=2    IO ▶
              → analyze_with_code2llm(project_path:Path) → dict CC=8    IO
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/4 pure  BOTTLENECK: analyze_with_code2llm(CC=8)

  Examples [Analysis]: ? → ?
              → main()                                  CC=3    IO ▶
              → run_mcp_workflow(code_path:Path, max_iterations:int) CC=5    IO
              → analyze_with_code2llm(code_path:Path) → dict CC=9    IO
              → log_step(step:int, description:str)     CC=1    IO ■
              PURITY: 0/4 pure  BOTTLENECK: analyze_with_code2llm(CC=9)

  Src [NLP]: Path → Optional[Language]
              → check(file:Path, language:Optional[str]) CC=8    IO ▶
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/3 pure  BOTTLENECK: check(CC=8)

  Src [NLP]: list[Path] → Optional[Language]
              → batch(paths:list[Path], recursive:bool, include:Optional[str], exclude:Optional[str], use_gitignore:bool, output_format:str, fail_fast:bool) CC=42   IO  !! ▶
              → detect_language(source:str | Path) → Optional[Language] CC=5    pure
              → from_path(cls, path:str | Path) → Optional[Language] CC=1    pure ■
              PURITY: 2/3 pure  BOTTLENECK: batch(CC=42)


TRANSFORMS (fan-out ≥10):
  batch(paths:list[Path], recursive:bool, include:Optional[str], exclude:Optional[str], use_gitignore:bool, output_format:str, fail_fast:bool) fan=34   !! mutation-heavy
  validate(code:Optional[str], file:Optional[Path], language:Optional[str], reference:Optional[Path], config:Optional[Path], enable_semantic:bool, enable_security:bool, model:Optional[str], output_format:str, verbose:bool) fan=20   !! side-effects
  _parse_response(response_text:str) → ValidationResult   fan=17   PIPELINE:SemanticValidator.entry
  analyze_with_code2llm(code_path:Path) → dict            fan=16   fan=16
  run_mcp_workflow(code_path:Path, max_iterations:int)    fan=16   fan=16
  run_simple_workflow(code_path:Path, max_iterations:int) fan=15   fan=15
  analyze_with_code2llm(code_path:Path) → Dict            fan=13   fan=13
  check(file:Path, language:Optional[str])                fan=12   fan=12
  main()                                                  fan=12   !! script-in-disguise
  main()                                                  fan=12   !! script-in-disguise
  main()                                                  fan=11   !! script-in-disguise
  _run_docker(code:str, language:str) → ExecutionResult   fan=11   PIPELINE:SandboxRunner.entry
  main()                                                  fan=11   !! script-in-disguise
  info()                                                  fan=10   fan=10
  _try_bandit(code:str) → list[Issue]                     fan=10   PIPELINE:SecurityValidator.entry

CONTRACTS:
  Pipeline: Src
    validate(code:Optional[str], file:Optional[Path], language:Optional[str], reference:Optional[Path], config:Optional[Path], enable_semantic:bool, enable_security:bool, model:Optional[str], output_format:str, verbose:bool)
      IN:  code:Optional[str], file:Optional[Path], language:Optional[str], reference:Optional[Path], config:Optional[Path], enable_semantic:bool, enable_security:bool, model:Optional[str], output_format:str, verbose:bool
      OUT: None
      SIDE-EFFECT: IO(read_text, read_text, print)
      INVARIANT: raises on invalid input
      SMELL: CC=18 → split

    get_language_for_validation(source:str | Path, explicit:Optional[str]) → str
      IN:  source:str | Path, explicit:Optional[str]
      OUT: str

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]

  Pipeline: Src
    validate(proposal:Proposal, context:dict) → ValidationResult
      IN:  proposal:Proposal, context:dict
      OUT: ValidationResult
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    _check_lizard(code:str, language:str, filename:str | None) → tuple[float, list[Issue], dict]
      IN:  code:str, language:str, filename:str | None
      OUT: tuple[float, list[Issue], dict]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]

  Pipeline: Src
    validate(proposal:Proposal, context:dict) → ValidationResult
      IN:  proposal:Proposal, context:dict
      OUT: ValidationResult
      INVARIANT: raises on invalid input

    _call_llm(prompt:str) → str
      IN:  prompt:str
      OUT: str

    _call_ollama(prompt:str) → str
      IN:  prompt:str
      OUT: str

    _call_http(prompt:str) → str
      IN:  prompt:str
      OUT: str
      SIDE-EFFECT: IO(loads, dumps, read)

  Pipeline: Src
    validate(proposal:Proposal, context:dict) → ValidationResult
      IN:  proposal:Proposal, context:dict
      OUT: ValidationResult
      INVARIANT: raises on invalid input

    _validate_treesitter(code:str, language:str) → tuple[float, list[Issue]]
      IN:  code:str, language:str
      OUT: tuple[float, list[Issue]]
      SIDE-EFFECT: pure
      INVARIANT: raises on invalid input

    tree_sitter_error_count(code:str, language:str) → int
      IN:  code:str, language:str
      OUT: int
      SIDE-EFFECT: global(errors)

    parse_code(code:str, language:str)
      IN:  code:str, language:str
      OUT: None

  Pipeline: Src
    matches(path:Path | str) → bool
      IN:  path:Path | str
      OUT: bool

    _match_pattern(rel_path:str, name:str, pattern:str) → bool
      IN:  rel_path:str, name:str, pattern:str
      OUT: bool

    _fnmatch(name:str, pattern:str) → bool
      IN:  name:str, pattern:str
      OUT: bool

    _pattern_to_regex(pattern:str) → str
      IN:  pattern:str
      OUT: str
      SIDE-EFFECT: pure

  Pipeline: Src
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    diff_python_code(before_code:str, after_code:str) → GraphDiffResult
      IN:  before_code:str, after_code:str
      OUT: GraphDiffResult

    diff_graphs(before:CodeGraph, after:CodeGraph) → GraphDiffResult
      IN:  before:CodeGraph, after:CodeGraph
      OUT: GraphDiffResult

    _diff_list(before, after, added:bool)
      IN:  before, after, added:bool
      OUT: None

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    run_simple_workflow(code_path:Path, max_iterations:int)
      IN:  code_path:Path, max_iterations:int
      OUT: None
      SIDE-EFFECT: IO(read_text, print, print)

    run_simple_test(code:str) → Dict
      IN:  code:str
      OUT: Dict
      SIDE-EFFECT: IO(print, print)

    log_step(step:int, description:str)
      IN:  step:int, description:str
      OUT: None
      SIDE-EFFECT: IO(print)

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    test_language_detection()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    analyze_with_code2llm(project_path:Path) → dict
      IN:  project_path:Path
      OUT: dict
      SIDE-EFFECT: IO(print, print, print)

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]

  Pipeline: Examples
    main()
      IN:  ()
      OUT: None
      SIDE-EFFECT: IO(print, print, print)

    run_mcp_workflow(code_path:Path, max_iterations:int)
      IN:  code_path:Path, max_iterations:int
      OUT: None
      SIDE-EFFECT: IO(read_text, print, print)

    analyze_with_code2llm(code_path:Path) → dict
      IN:  code_path:Path
      OUT: dict
      SIDE-EFFECT: IO(print, print, print)

    log_step(step:int, description:str)
      IN:  step:int, description:str
      OUT: None
      SIDE-EFFECT: IO(print)

  Pipeline: Src
    check(file:Path, language:Optional[str])
      IN:  file:Path, language:Optional[str]
      OUT: None
      SIDE-EFFECT: IO(read_text, print, print)
      INVARIANT: raises on invalid input

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]

  Pipeline: Src
    batch(paths:list[Path], recursive:bool, include:Optional[str], exclude:Optional[str], use_gitignore:bool, output_format:str, fail_fast:bool)
      IN:  paths:list[Path], recursive:bool, include:Optional[str], exclude:Optional[str], use_gitignore:bool, output_format:str, fail_fast:bool
      OUT: None
      SIDE-EFFECT: IO(print, print, print)
      SMELL: CC=42 → split

    detect_language(source:str | Path) → Optional[Language]
      IN:  source:str | Path
      OUT: Optional[Language]

    from_path(cls, path:str | Path) → Optional[Language]
      IN:  cls, path:str | Path
      OUT: Optional[Language]


DATA_TYPES (by cross-function usage) [151 annotated, 0 inferred / 223 functions]:
  str                  consumed:116 produced:19   !! HUB-TYPE → split interface
  dict                 consumed:21  produced:9    !! HUB-TYPE → split interface
  bool                 consumed:10  produced:13   !! HUB-TYPE → split interface
  Proposal             consumed:19  produced:0    !! HUB-TYPE → split interface
  ValidationResult     consumed:0   produced:18   output-heavy
  Path                 consumed:17  produced:0    !! HUB-TYPE → split interface
  str, Any             consumed:8   produced:4  
  float                consumed:3   produced:6  
  int                  consumed:5   produced:3  
  VallmSettings        consumed:6   produced:1    input-heavy (read-only flow)
  list[dict]           consumed:1   produced:5    output-heavy
  Dict                 consumed:3   produced:3  
  Language             consumed:0   produced:4    narrow scope
  GitignoreParser      consumed:1   produced:2    narrow scope
  list                 consumed:2   produced:1    narrow scope
  list[Issue]          consumed:0   produced:3    narrow scope
  str | Path           consumed:3   produced:0    narrow scope
  ExecutionResult      consumed:0   produced:3    narrow scope
  str | None           consumed:1   produced:1    narrow scope
  set                  consumed:2   produced:0    narrow scope

  HUB TYPES (consumed ≥10):
    str → 116 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    dict → 21 consumers → split into:
      - replace with typed alternatives (dataclass/TypedDict)
    bool → 10 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    Proposal → 19 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)
    Path → 17 consumers → split into:
      - (analyze consumers to suggest sub-interfaces)

SIDE_EFFECTS:
  IO:        main, validate, check, batch, info, _check_dep, _output_rich, _output_json, _output_text, SecurityValidator._try_bandit
  Mutation:  validate, _get_default_validators, ComplexityValidator.__init__, ComplexityValidator.validate, ComplexityValidator._check_python_complexity, ComplexityValidator._check_lizard, SecurityValidator.validate, SecurityValidator._check_patterns, SecurityValidator._check_python_ast, SemanticValidator.__init__
  Pure:      bump_version, VallmSpec.validate_proposal, VallmSpec.get_validator_name, VallmSpec.get_validator_tier, VallmSettings.from_toml, Issue.__str__, compute_verdict, BaseValidator.validate, SecurityValidator._get_func_name, SemanticValidator.validate

  PIPELINE PURITY:
    Src             ███░ 75% pure
    Src             ██░░ 50% pure
    Src             ███░ 75% pure
    Src             ██░░ 50% pure
    Src             ███░ 75% pure
    Src             ███░ 75% pure
    Examples        ░░░░ 0% pure
    Examples        ██░░ 50% pure
    Examples        ██░░ 50% pure
    Examples        ░░░░ 0% pure
    Src             ██░░ 66% pure
    Src             ██░░ 66% pure
